import React from 'react';
// Tên component (Functional component)

export const Card = () => {
  // Nội dung của thẻ card component (chỉ được bao phủ bởi 1 thẻ duy nhất)
  return <div>Đây là thẻ card (react functional component) - bc64</div>;
};
